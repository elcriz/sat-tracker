{
  "name": "sat-tracker-server",
  "version": "1.0.0",
  "description": "A simple radio satellite tracker",
  "main": "server.js",
  "author": "Christiaan Hemerik",
  "license": "ISC",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": "16.15.1"
  }
}
